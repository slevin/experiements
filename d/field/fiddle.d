#!/usr/local/bin/rdmd

   //--compiler=ldmd2

enum size_t sz = 100000;

void main(string[] args) {
    vec slopes[sz];
}

struct Field2 {
    vec slopes[sz];
}

struct vec {
    float x;
    float y;
}

// same with int?

// get rid of y and its 2.5 seconds
